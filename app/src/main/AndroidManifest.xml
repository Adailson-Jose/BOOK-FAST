<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projeto.bookfast.book_fast">

    <application
        android:allowBackup="true"
        android:icon="@drawable/bookfast"
        android:label="Book Fast"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".GUI.TelaLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GUI.TelaInicial" />
        <activity android:name=".GUI.TelaRecuperarSenha" />
        <activity android:name=".GUI.TelaCadastrar"></activity>
    </application>

</manifest>